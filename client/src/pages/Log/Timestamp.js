import React from 'react';
import { render } from 'react-dom';
import Timestamp from 'react-timestamp';

const TimestampButton = props => {
    let date = new Date(); // Date in JSON format in UTC
  

  };
